const DocumentsPage = () => {
      return (  <>
      this is a protected doc page.
      </>);
}
 
export default DocumentsPage;
// in order to prevent user to have direct access to the documents folder i'll create a layout for it.